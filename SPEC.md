# the-boxes 数据格式规范

> SPEC v2.0 · 2026-09-19 · 修改必须走文末变更日志 · 解析器必须对未知语法容错（§7）

## 1. 状态语义

> 点击在三态间循环 `[ ] → [/] → [x]`，到 `[x]` 停住。
> 迁移（把 todo 挪到另一天）是**物理移动**：整行**原样**搬到目标日文件
> （状态、`@start`/`@done`、`+任务`、`^id` 全部保留，todo 身份不变），源文件删除该行。
> 界面操作：把 todo 拖到左侧日期列表的某一天。

| 状态 | 名称 | 语义 | 出现在"今日"视图的条件 |
|---|---|---|---|
| `[ ]` | 待办 | 提出后未处理 | 所在文件为今日文件 |
| `[/]` | 进行中 | 正在做 | 所在文件为今日文件 |
| `[x]` | 完成 | 已完成 | 不出现 |

## 2. 视图聚合规则

```
某日视图 = 该日 inbox 文件中的全部 todo 行
今日视图 = 今日文件中的 [ ] 与 [/]
```

- 已完成 `[x]` 不出现在今日视图
- 昨日未处理的 `[ ]` **不**自动滚入今日：显式迁移（拖到某天，整行移动）才移动——每日回顾 inbox 是核心动作（Q7 假设，M1 验证）

## 3. 目录结构

```
~/the-boxes/
├── inbox/
│   └── YYYY-MM-DD.md      # 一天一文件：当天提出的所有 todo
├── tasks/
│   └── <slug>.md          # 一任务一文件
├── archive/               # 完结任务归档（从 tasks/ 移入）
└── .boxes.yml             # 工具配置（M3 起）
```

## 4. inbox 行格式

```
- [状态] 文本 [+任务slug] [@start:YYYY-MM-DD] [@YYYY-MM-DD] [@done:YYYY-MM-DD] [^id]
```

- 元数据 token 顺序不限，解析按 token 识别；界面写入时用上述规范顺序
- `@start:YYYY-MM-DD`：`[/]` 切到进行的开始日期，界面在切到进行时自动写入；完成后保留
- `@YYYY-MM-DD`：可选意向日期，纯备注；迁移不再写此 token（整行移动，落点即文件）
- `@done:YYYY-MM-DD`：可选，完成日期，界面标记完成时自动写入
- `+任务slug`：可选，归属任务（行不移动，任务视图聚合显示）
- `^id`：短哈希 ID，见 §6；迁移（整行移动）保留原 id

示例：

```markdown
# 2026-09-18

- [ ] 回复 Alice 的邮件 ^k3f9
- [/] 写 the-boxes 的 SPEC ^a02x
- [x] 晨跑 30 分钟 @done:2026-09-19 ^b7q1
- [ ] 修剪草坪 ^c8m2
- [ ] 买生日礼物 @2026-09-22 ^d4n7
- [ ] 挑瓷砖 +装修 ^e5p8
```

## 5. 任务文件格式

`tasks/<slug>.md`，slug 为小写文件名（中文可用，避免空格）：

```markdown
# 任务：装修

**目标**：11 月底前完成硬装
**状态**：进行中
**提出**：2026-09-19

## todos

- [ ] 约师傅看现场 ^f6r3
- [ ] 买乳胶漆 @2026-10-05 ^g7s4
```

- **目标**：一句话，任务的存在的理由
- **状态**：`进行中` | `已完成` | `搁置`；只有 `进行中` 参与今日聚合
- **提出**：ISO 日期
- 任务内 todo 与 inbox 同一套状态语义与聚合规则
- 归档 = 移动文件至 `archive/`（内容不变）

## 6. ID 规则

- 创建时由「内容 + 提出时间」哈希取 4 位 base36，形如 `^a3f9`；碰撞重取
- 界面创建的行必带；手写行可选（无 ID 的行以内容定位，改名即失联）
- 命令与接口引用 todo 时优先使用 ID

## 7. 容错与兼容

- 未知行、未知 token：**原样保留**，解析时忽略，不算错误（v1.x 的 `[>]` / `[<]` 行在此规则下静默保留，由用户自行改写或删除）
- 解析失败的行：保留原文，`doctor` 体检时报告
- Obsidian 兼容：文件即标准 Markdown，可任意在 Obsidian / VS Code 编辑；Obsidian Tasks 的 emoji 语法不解析、不破坏

## 8. 配置 .boxes.yml（M3 起）

```yaml
backup:
  git: git@github.com:you/boxes-data.git
  webdav:
    url: https://dav.example.com/boxes/
    retain: 7
```

## 变更日志

- **2026-09-19** v2.0：删除 `[>]` / `[<]`，迁移改为拖拽。修改建议：五态中 `[>]` / `[<]` 只为"迁移留痕"存在，但留痕行不参与任何视图，还衍生出顺延/排期两套对称语义与「迁移到今天/以后」二选一菜单，认知与实现成本高；迁移入口改为"拖到左侧日期列表"后，留痕失去必要性。实施方案：①状态缩为三态 `[ ]` `[/]` `[x]`，`[>]` / `[<]` 行按 §7 未知语法容错（原样保留、解析忽略）；②迁移 = 整行**原样移动**到目标日文件（状态、`@start`/`@done`、`+任务`、`^id` 全保留，身份不变），源文件删行，目标文件不存在则创建；③`@日期` 降级为普通可选 token（意向日期），不再与迁移绑定；④界面改为左侧日期列表（「接下来」= 今天起 7 天 + 「更早」= 已有文件，倒序）+ 右侧任务列表，拖放目标即侧栏日期项，drop 高亮反馈。
- **2026-09-19** v1.3：迁移改为物理移动。修改建议：原语义"迁移只改原行状态、`@日期` 参与聚合"导致目标日期文件无对应待办，切到该日看不到迁来的任务。实施方案：①迁移 = 原行改 `[>]`/`[<]` 留原文件作记录（写 @目标日）+ 目标日文件新建同名 `[ ]`（保留 `+任务`，新 `^id`，不带 `@start`/`@done`），目标文件不存在则创建；②§2 聚合改为"视图 = 当日文件内容"，`[>]`/`[<]` 记录行不参与任何聚合；③示例改为记录行视角（原文件 2026-09-18）。界面配套：迁移菜单仅对 `[ ]`/`[/]` 开放（防记录行重复迁移产生副本），查看今日文件时隐藏"迁移到今天"。（v2.0 已撤销本版语义）
- **2026-09-19** v1.2：新增 `@start:YYYY-MM-DD` token。切到 `[/]` 写开始日期，完成后保留并展示"始于…，完成于…"；回退到 `[ ]` 时清除。行格式补充该 token，界面显示规则更新。
- **2026-09-19** v1.1：调整状态语义与交互。点击只在三态 `[ ] → [/] → [x]` 循环、到完成停住；`[>]`/`[<]` 不再点击产生，改为"过期任务 → 悬停迁移菜单"：迁移到今天 = `[>]`+今天，迁移到以后 = `[<]`+所选日（两个都写 `@日期`）。
- **2026-09-19** v1.0：初版冻结。状态语义（BuJo 式：`[>]` 顺延滚动 / `[<]` 排期绑日期）、今日聚合规则（不自动滚动，显式顺延）、行格式（`@日期` / `@done` / `+任务` / `^id` token）、任务文件格式、短哈希 ID 规则、容错与兼容规则。

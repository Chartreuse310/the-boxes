# the-boxes · 界面风格规则（Style Guide）

> 版本 v0.3.4 · 2026-09-19 · **本文是界面视觉的唯一事实源。**
> `src/styles.css` 必须服从本文；两者不一致时，以本文为准，并视为待修复缺陷。
> 视觉改动流程：先改本文 → 再改 `styles.css` → 在文末变更日志追加一条。

## 0. 版权与来源

- 本项目**沿用 [obsidian-minimal](https://github.com/kepano/obsidian-minimal) 的设计方法**，而非复制其代码或主题文件。所沿用的方法为四条：① 全部中性色由单一 HSL 基准派生；② system-first 字体栈，不打包字体；③ 圆形 checkbox；④ 完成项默认无删除线。
- 其余一切——基准色值、语义色、字号、间距、圆角、阴影、动效——均为 the-boxes 自身的选择，与 Minimal 无关。**不要把本文的数值反推为 Minimal 的数值。**
- obsidian-minimal 为 **MIT License，Copyright (c) 2020-2024 Steph Ango (@kepano)**。若日后引入其具体实现（如 checkbox SVG 图标），须连同上述版权声明一并保留。
- 本项目自身为 MIT License，Copyright (c) 2026 Chartreuse310。

## 1. 设计理念

1. **内容是唯一的主角**：不装饰元素。用留白、缩进与字重表达层级，而不是用边框、阴影、底色。
   每新增一个视觉元素，先回答"它承载了什么信息"；答不出就不加。
2. **两个信号胜过三个弱信号**：当某个元素"浮不起来"时，正确做法是**去掉它**或加强唯一手段，而不是叠加边框＋阴影＋底色去补偿。
3. **单一色相派生**：全部中性色由一个 HSL 基准派生。改基准即改整套配色。
4. **语义色只做强化，符号才是主通道**：颜色不得是唯一的信息载体（色觉障碍、灰度屏、打印均需可读）。
5. **易读性优先于美观**：任何对比度不达标的"高级感"都是缺陷，见 §2.3。
6. **为反馈而动，不为表演而动**：动效上限 150ms（§2.4）。
7. **日日用的"轻"**：一屏看得下更多，比看得更精致重要。

## 2. 设计 Token

### 2.1 中性色（由基准派生，唯一基准）

```css
--base-h: 38;      /* 暖纸色相：比纯灰柔和，降低长时间注视的冷硬感 */
--base-s: 12%;
--base-l: 97%;     /* 页面底色 = 基准本身 */
```

所有中性色 = `hsl(var(--base-h) var(--base-s) <基准亮度 − 偏移>)`。

| Token | 偏移 | 实际值 | 用途 |
|---|---|---|---|
| `--bg-page` | 基准 | `#f8f8f6` | 页面背景 |
| `--bg-surface` | 固定白 | `#ffffff` | 浮层、输入框、浮起表面 |
| `--bg-hover` | l−4 | `#efeeeb` | 行悬停底色 |
| `--bg-sunken` | l−6 | `#ebe9e5` | 内嵌代码、弱底衬、「今日」标记底 |
| `--divider` | l−12 | `#dddad4` | 分隔线（**非组件边界**，不受 §2.3 约束） |
| `--border-control` | l−43 | `#988d7c` | 交互控件描边（**须 ≥3:1**） |
| `--text-secondary` | l−58 | `#6f6758` | 次级文本、元数据、完成项文字、box 描边与符号 |
| `--text` | l−88 | `#1a1814` | 正文、标题 |

**深色 token 必须对四种表面同时达标，取最严者。** 本项目最暗的表面是 `--bg-sunken`，它决定下限：

| Token | `--bg-page` | `--bg-hover` | `--bg-sunken` | `--bg-surface` | 判定 |
|---|---|---|---|---|---|
| `--text` | 16.67:1 | 15.28:1 | 14.62:1 | 17.73:1 | ✅ |
| `--text-secondary` `#6f6758` | 5.26:1 | 4.82:1 | 4.61:1 | 5.59:1 | ✅ |
| ~~`#786f5e`（v0.3 初稿）~~ | 4.66:1 | 4.27:1 | 4.09:1 | 4.96:1 | ❌ 悬停即跌破 4.5:1 |
| `--border-control` `#988d7c` | 3.07:1 | 2.81:1 | 2.69:1 | 3.26:1 | ⚠️ 仅可在 page / surface 上 |

- `--border-control` 只对 page 与 surface 达标，因此**交互控件不得放在 `--bg-hover` / `--bg-sunken` 表面上**。
- **只有两个文本层级。** 比 `l−58` 更浅的灰字在 `--bg-sunken` 上无法达到 4.5:1，因此不存在"第三级更淡的灰字"——需要更弱时，用**字号**（11px）和**字重**（400）降级，不要用更浅的颜色。

### 2.2 语义色（仅两个，固定值，不参与派生）

| Token | 值 | 唯一含义 | 阈值 |
|---|---|---|---|
| `--c-done` | `#4f7a33` | **表达"完成"**（状态框填充、完成度进度条） | 作图形 ≥3:1：page 4.74 / sunken 4.16 ✅ |
| `--on-done` | `#ffffff` | 完成勾的前景（在 `--c-done` 实心上） | 5.04:1 ✅ |
| `--accent` | `#0e7490` | **应用主色**：品牌标记 + 焦点环 + 主操作 | 作图形 ≥3:1：5.04:1 ✅ |
| `--accent-focus` | `hsla(193 82% 31% / 0.16)` | 焦点环专用 | 即 `--accent` 本身 16% alpha |

规则：**语义色的含义必须唯一，且不得跨义复用。**

- `--c-done` 的语义是"完成"。凡**表达完成度**的场景都可用（状态框、进度条填充）——此时颜色与含义一致，是强化而非复用。
- 用 `--c-done` 表达**其他含义**则禁止。典型反例：「今日」标记若用绿色，会让"绿 = 完成"的约定失效。
- `--accent` 是应用主色，**不表示任何状态**：不得用青绿表示"进行中""已完成"或任何五状态之一。
- 其余全部状态差异由符号承担（§4.2），不新增状态色。
- `--accent-focus` 必须由 `--accent` 派生（注意 `#0e7490` 实为 `hsl(193 82% 31%)`，不要照抄近似值）。

### 2.3 对比度硬约束（可机械验证）

阈值取 WCAG 2.1 AA。**比较基准是"该元素实际会落在的每一种表面"中最不利的那一种**，而不是只算页面底色：

| 对象 | 阈值 | 说明 |
|---|---|---|
| 正文与元数据文本（任意字号） | **≥ 4.5:1** | 本项目最小字号 11px，不享受"大字号 3:1"豁免 |
| 交互控件边界（下拉、输入框、日期框） | **≥ 3:1** | WCAG 1.4.11 非文本对比度 |
| 状态符号与图标（含状态框填充、进度条填充） | **≥ 3:1** | 同上 |
| 纯分组用分隔线、悬停底色 | 无要求 | 不承载信息，不可作为唯一区分手段 |

务必注意：**悬停底色本身会降低对比度**（`--bg-hover` 比 `--bg-page` 暗，`--bg-sunken` 更暗）。因此"在页面上看起来够黑"不等于合格——必须把 `--bg-hover` / `--bg-sunken` 一并代入。v0.3 初稿的 `--text-secondary`（`#786f5e`）正是在这一步被查出：页面上 4.66:1 合格，**鼠标一悬停行就跌到 4.27:1**。

新增或修改任何颜色后，必须重新核算其**全部可能表面**的对比度并在变更日志记录实测值。

### 2.4 圆角、阴影、动效

```css
--radius-row: 8px;       /* 行（仅用于悬停底色，行本身无边框） */
--radius-control: 8px;   /* 下拉、输入框、菜单项 */
--radius-pop: 12px;      /* 浮层 */
--radius-pill: 999px;    /* 胶囊（如使用） */

--shadow-pop: 0 8px 24px -10px rgba(31, 28, 23, 0.18);  /* 仅浮层 */

--dur-fast: 120ms;       /* 悬停、颜色、透明度 */
--dur-base: 150ms;       /* 上限，不得超出 */
--ease: cubic-bezier(0.22, 0.61, 0.36, 1);
```

- **阴影只有一个，且只用于浮层。** 列表行、卡片、容器一律无阴影。
- 动效只允许 `color / background-color / opacity / transform: scale()`。
  **禁止位移**（`translateY`、`translateX`）、拉长、旋转、扫光。
- 禁止逐行错峰入场（stagger）。列表淡入若保留，整体一次性 ≤120ms。

### 2.5 字体栈

```css
--font-stack: -apple-system, BlinkMacSystemFont, 'Segoe UI', Inter, Ubuntu,
  'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', sans-serif;
```

- system-first，不打包字体文件（原生感与性能，沿用 Minimal 方法）。
- `-webkit-font-smoothing: antialiased` 常开。
- 数字不强求等宽；但计数、进度等会跳动的数字用 `font-variant-numeric: tabular-nums`。

## 3. 字号与层级

四个层级，对应四种角色（不绑定具体元素，允许元素换位）：

| Token | 字号 | 字重 | 角色 |
|---|---|---|---|
| `--fs-title` | 17px | 600 | 页面标题 |
| `--fs-body` | 15px | 400 | todo 正文 |
| `--fs-label` | 12px | 500 | 菜单项、按钮、次级标签 |
| `--fs-meta` | 11px | 400 | 元数据、日期、注记 |

- 正文下限 14px，元数据下限 11px，**低于 11px 禁止**。
- 行高：正文 1.5；元数据 1.4。
- 字重只用 400 / 500 / 600 三档，且只用于上表规定的位置。
- 中文加粗在 mac 上易糊，`600` 仅用于标题；不要用 700。

## 4. 间距与尺寸

基准网格 **4px**。下表为规定用法，不得自行取整到 5/6/7 等非 4 倍数。

| 用途 | 值 |
|---|---|
| 页面最大宽 | 640px，居中 |
| 页面上下内边距 | 48px 上 / 56px 下 |
| 页面左右内边距 | 20px（窄屏 16px） |
| 标题区与列表间距 | 20px |
| 行内边距 | 10px 上下 / 12px 左右 |
| 行最小高度 | 40px（保证点击区域） |
| 行间距 | 0（用分隔线，不用间隙堆叠） |
| 行内元素间距 | 12px |
| 分组之间 | 24px 以上，用留白而非分割线 |

### 4.1 行模型（本项目的核心规则）

**行不是卡片。** 每一行必须是：

- **无边框、无背景、无阴影**；整行可点，悬停时整行铺 `--bg-hover`，圆角 `--radius-row`。
- 与下一行之间用 **0.5px `--divider` 分隔线**，且**从文字起点缩进**（左侧留出状态框宽度）——这是"缩进分隔线"而非通栏横线，视觉上把状态框和内容分成两栏。
- 分隔线在悬停行及其**相邻行**上隐藏，避免与悬停底色打架。

```css
.todo { position: relative; padding: 10px 12px; min-height: 40px;
        border: none; background: none; box-shadow: none; }
.todo:hover { background: var(--bg-hover); }
.todo + .todo::before {           /* 缩进分隔线：left 对齐文字起点 = 12 + 18 + 12 */
  content: ''; position: absolute; left: 42px; right: 0; top: 0;
  height: 1px; background: var(--divider);
}
.todo:hover::before,
.todo:hover + .todo::before { opacity: 0; }
```

这条规则为什么重要：`--divider` 对页面底只有 1.31:1、`--bg-hover` 只有 1.09:1，两者都是弱信号。**不允许再叠加边框和阴影去"加强"它**——弱信号本就不该被加强，它只需要不干扰阅读。（v0.2 曾因叠加而退化为"卡片墙"，见变更日志。）

### 4.2 状态框

| 项 | 值 |
|---|---|
| 尺寸 | 18px × 18px（定值，不用 `calc`） |
| 形状 | 圆形（`border-radius: 50%`） |
| 描边 | 1.5px `--text-secondary` |
| 符号字号 | 12px / 500 |

| 状态 | 符号 | 填充 | 描边/符号色 |
|---|---|---|---|
| `[ ]` 待办 | 无 | 透明 | `--text-secondary` |
| `[/]` 进行中 | `/` | 透明 | `--text-secondary` |
| `[x]` 完成 | `✓` | `--c-done` 实心 | `--c-done` |
| `[>]` 顺延 | `>` | 透明 | `--text-secondary` |
| `[<]` 排期 | `<` | 透明 | `--text-secondary` |

- 符号沿用文件语法的字符，便于用户把界面与 Markdown 原文对应起来。
- **五个状态只有"完成"有颜色。** 四个未完结状态共用 `--text-secondary`，靠符号区分——这既是 §1.4 的要求，也让未完成的任务不会互相抢注意力。
- 符号不可省略：颜色不是唯一载体。
- 禁止用渐变模拟"半填充""进度填充"等效果（v0.2 的 `--c-doing-tint` 已删除）。

**符号笔画宽度（机械可验证）**

| 项 | 值 | 来源 |
|---|---|---|
| `/` 笔画宽 | 0.78px | 实测（12px/500 字形轮廓） |
| `>` `<` 单臂笔画宽 | 0.94px | 实测（同上，取单臂垂直剖面） |
| 完成勾 `stroke-width` | **0.9px** | 落在 0.78–0.94 带内，与两个折线符号最接近 |
| 完成勾居中偏差 | **0.00px** | 墨迹包围盒中心 = viewBox 中心 (6,6) |

- 量法：把 `src/styles.css` 内联到只含状态框的探针页，`--force-device-scale-factor=16` 截图，从实底/实字色反解**覆盖率场 α**（不是二值阈值），取 α≥0.5 为几何真边，再用**垂直剖面**量 0.5 交叉点间距。
- **不要用最大内切圆（EDT）量折线符号的线宽**：折角处的最大内切圆会横跨两条臂，实测把 1.30px 的勾读成 1.56px。EDT 只对无拐角的直棒（如 `/`）可用。
- 完成勾必须用矢量绘制，不得用 CSS 边框拼 L 形（`border-left` + `border-bottom` + `rotate(-45deg)`）：
  - 线宽会等于边框宽度，而 1px 边框的实际墨迹是 2 条各 1px 的边，勾因此变成 2px，比所有字形都粗一倍；
  - L 形绕自身中心旋转 45° 后，**墨迹包围盒中心与旋转原点不重合**（v0.3 实测偏左 1.4px、偏下 0.7px），只能靠 `translate` 补一个随尺寸漂移的魔数。
  - 矢量折线（`<polyline>`，`stroke-linecap/linejoin: round`）的墨迹包围盒天然以 viewBox 中心为心，flex 居中即勾居中，且线宽是独立参数、不随容器尺寸变化。

## 5. 组件规则

### 5.1 元数据（任务、日期、注记）

- **一律 11px / 400 / `--text-secondary`，右对齐，最多 2 项，超出省略。**
- **不使用彩色底 + 深色字的胶囊。** v0.2 的 `--chip-task-*` / `--chip-date-*` 四个色值已删除；两个色相只为区分"任务"和"日期"而存在，性价比为负。
- 界面上去掉语法前缀：`+装修` 显示为 `装修`，`@2026-09-22` 显示为 `9/22`。文件里的 `+` / `@` 是数据语法，不是给用户看的文案。
- 日期短格式：同年 `M/D`；非同年 `YYYY/M/D`。**适用于下拉框选项与右对齐的元数据**（`value` 保持 ISO，只改显示）。
- **不使用 emoji 作为界面元素**（跨平台字形不一致，且违反 §1.1 克制原则）。
  用户在 todo 文本里自己写的 emoji 原样显示，不属于界面元素。
- 不使用破折号、括号等连接词拼装注记；多项元数据靠间距和对齐区分。
- **起止日期注记是上述两条的既定例外**（唯一一处）：

  | 数据 | 显示 |
  |---|---|
  | 有开始日（可含完成日） | `——始于 YYYY-MM-DD 🛫`，有完成日时续 `，完成于 YYYY-MM-DD 🎉` |
  | 只有完成日 | `——完成于 YYYY-MM-DD 🎉` |

  例外成立的理由：注记是**面向文件**的行内说明，不是界面控件。它用文件的 ISO 日期（与 inbox 原文一一对应）、并保留用户自己的 `——` / `🛫` / `🎉` 记号，因此不适用"界面不用 emoji""日期短格式"两条。
  同日不特殊处理——注记是原样陈述起止，不做"折叠"这类加工（v0.3.3 一度改成 `9/18 → 9/19` 并按同日折叠，已按用户要求撤销）。

### 5.2 焦点态

- 可聚焦元素统一 `outline: none` + `box-shadow: 0 0 0 3px var(--accent-focus)`。
- **不得移除焦点样式**而不提供替代。键盘操作是核心路径（PLAN 北极星指标含"状态切换 ≤2 秒"）。

### 5.3 浮层（菜单、日期选择）

- `--bg-surface` + `1px solid var(--border-control)` + `--radius-pop` + `--shadow-pop`。
- 菜单项：12px / 500，内边距 8px 10px，悬停 `--bg-hover`。
- 回车/点击外部关闭；打开时焦点进入浮层第一项。

## 6. 禁止项

- 页面或容器背景使用渐变、纹理、噪点、模糊、磨砂。
- 行、卡片、容器使用 `box-shadow`（唯一例外：浮层）。
- 悬停时元素发生位移（`translate`）。
- 硬编码色值出现在组件规则中（`#hex`、`hsla()` 字面量）——**必须走 §2 的 token**。
- 动效时长 > 150ms，或逐行错峰入场。
- 语义色它用（§2.2）。
- 字号 < 11px。
- 界面文案暴露实现细节（文件路径、环境变量名、版本号、内部状态枚举名）。
  - **例外一：footer。** footer 显示「数据目录 · 软件版本 · 数据格式版本」，不带 `数据` / `数据格式` 这类标签前缀。用户需要知道数据落在磁盘哪里、自己跑的是哪一版——这属于产品主张（数据在自己手里），不是实现泄漏。
  - **例外二：空态的来源文件。** 空态必须写出 todo 的来源文件全名（`…/inbox/2026-09-19.md`）——"去文件里加一行"是本产品的基本用法，文件名本身就是操作指引，省略掉用户就无从下手。
  - **两处例外都必须用接口给的值，不得硬编码。** 取 `GET /api/info`（开发期由 `vite.config.ts` 现读 `package.json` 与 `SPEC.md`）。v0.3.2 的 footer 把 `SPEC v1.0` 写死在界面里，SPEC 升到 v1.2 后界面就开始说谎；空态写死默认路径 `~/the-boxes`，配过 `.env` 的用户会看到一条指向不存在文件的指引。**值的单一来源**是这条规则的实质，不是措辞问题。
  - footer 与空态之外的任何位置不得出现路径、环境变量名、版本号。
  - 状态名对外一律用中文（待办 / 进行中 / 完成 / 顺延到今日 / 排期到以后）。`done` / `scheduled` 这类内部枚举名不得出现在可见文案，也不得出现在 `aria-label`。
  - 状态名对外一律用中文（待办 / 进行中 / 完成 / 顺延到今日 / 排期到以后）。`done` / `scheduled` 这类内部枚举名不得出现在可见文案，也不得出现在 `aria-label`。

## 7. 交付前检查清单（每项均可机械验证）

- [ ] 颜色是否全部来自 §2 的 token？组件规则里零 hex / 零 `hsla()` 字面量？
- [ ] 所有文本对其**全部可能表面**（page / hover / sunken / surface）的对比度 ≥ 4.5:1？
- [ ] 所有交互控件描边在其实际所在表面上 ≥ 3:1？（控件不得落在 hover / sunken 上）
- [ ] 状态差异在**灰度模式下**是否仍可区分？（把页面转成灰度截一张图检查）
- [ ] 是否有行/卡片带边框或阴影？（应全部为零）
- [ ] 动效是否全部 ≤ 150ms、无位移、无 stagger？
- [ ] 最小字号是否 ≥ 11px？
- [ ] 状态符号的笔画宽度是否与 `/` `>` `<` 字形同粗（实测带 0.78–0.94px）？
- [ ] 完成勾的墨迹包围盒中心是否与圆框中心重合（偏差应为 0.00px）？
- [ ] 行点击区域是否 ≥ 40px？
- [ ] 键盘 Tab 一圈下来，焦点环是否处处可见？
- [ ] 界面文案是否零实现细节（路径 / 环境变量 / 版本号 / 英文枚举）？只有 footer 与空态来源文件两处例外，且两处的值必须来自 `GET /api/info`，不得硬编码。
- [ ] footer 是否不带 `数据` / `数据格式` 这类标签前缀？
- [ ] 起止日期注记是否用文件的 ISO 日期与 `——始于 … 🛫，完成于 … 🎉` 原始措辞（§5.1 例外）？不要"折叠"或改成短日期。
- [ ] `aria-label` 是否也过了上一条？（读屏用户同样会听到内部枚举名，但看不到界面，问题更难被发现）

## 变更日志

- **2026-09-19 v0.3.4**：**按用户决定撤销 v0.3.3 的三处改动，并把它们改写成明确的例外条款。** v0.3.3 的一处失误是把"我认为更克制"的写法当成了规范——注记的措辞与记号是产品语气，属于用户，不属于我。
  ① §5.1 注记恢复为 `——始于 YYYY-MM-DD 🛫，完成于 YYYY-MM-DD 🎉`（只有完成日时 `——完成于 YYYY-MM-DD 🎉`）：**用文件的 ISO 日期**、保留 `——` / `🛫` / `🎉`，并撤销同日折叠。理由写入条款：注记是**面向文件**的行内说明而非界面控件，故不适用"界面不用 emoji""日期短格式"两条。日期短格式仍然只管下拉框与右对齐元数据。
  ② §6 空态恢复显示来源文件的**完整路径**，但改为取 `GET /api/info` 的**实际数据目录**（`~/the-boxes-data/inbox/2026-09-19.md`），不再写死默认路径 `~/the-boxes`。v0.3.3 把它裁成相对文件名是过度收敛——用户靠这行才知道去哪里加一行。
  ③ §6 footer 去掉 `数据` / `数据格式` 标签前缀，改为 `~/the-boxes-data · v0.0.1 · SPEC v1.2`。
  ④ §6 由"唯一例外"改为"两处例外"（footer + 空态来源文件），并重申实质：例外的正当性来自**值的单一来源**（都取接口），不是来自措辞放行。
  ⑤ §7 拆出三项检查：例外值是否来自接口、footer 是否残留标签前缀、注记是否被误改成短日期。
- **2026-09-19 v0.3.3**：**把 §6 / §5.1 从"CSS 一侧合规"推到界面文案一侧。** 起因：v0.3.2 只管住了 `styles.css`，`App.tsx` 仍有多处违规（§5.1 元数据只改了一半，`styles.css` 的注释里甚至写着"App 仍会挂 chip 类名"），是同一类文档↔实现漂移的复发。
  ① §6 补上例外条款：footer 的「数据 / 版本」一行允许出现数据目录与版本号，**但必须来自接口**（`GET /api/info` 现读 `package.json` / `SPEC.md`）。这条的实质是版本号的单一来源——v0.3.2 的 footer 把 `SPEC v1.0` 写死在界面里，SPEC 升到 v1.2 后界面就在说谎；
  ② §6 明确空态可以出现数据目录内的**相对文件名**（`inbox/2026-09-19.md`），因为"去文件里加一行"是产品基本用法，但不得出现绝对路径或 `~/…`；并明确状态名对内外一律用中文，`aria-label` 同样受约束（原 footer 的 `~/the-boxes/inbox/…` 是默认值，与实际 `.env` 配置不符）；
  ③ §5.1 补齐日期短格式的适用范围（下拉框选项、注记），并新增起止日期注记的**唯一写法表**（同日不写区间、分隔用 `→`、不用 `——`、不用 emoji）；
  ④ 删除 footer 的环境变量名与「开发模式」字样；
  ⑤ §7 检查清单拆分出 `aria-label` 一项——读屏用户会听到内部枚举名，但看不见界面，问题更难被发现。
- **2026-09-19 v0.3.2**：**新增「符号笔画宽度」硬规则，并订正勾的画法。** 起因：完成勾用 CSS 边框拼 L 形，实测线宽 2px（所有字形的一倍粗）且偏左 1.4px / 偏下 0.7px。改为矢量折线并以实测校准线宽：
  ① §4.2 新增笔画宽度表：`/` 0.78px、`>` `<` 单臂 0.94px、完成勾 `stroke-width` **0.9px**、居中偏差 **0.00px**；
  ② 写明量法（内联真实 CSS 的探针页 + 16 倍设备像素比 + 覆盖率场 α≥0.5 + 垂直剖面），并**警告不要用最大内切圆量折线符号**——折角处内切圆横跨两臂，会把 1.30px 读成 1.56px；
  ③ 禁止用 CSS 边框拼 L 形画勾，附两条理由（线宽不可独立设置；旋转后包围盒中心与旋转原点不重合，需魔数补正）；
  ④ §7 检查清单增加两项符号几何检查。
- **2026-09-19 v0.3.1**：**修正 v0.3 自身的两个对比度错误。** 引入"多表面核算"规则后发现：
  ① `--text-secondary` 由 `l−55`（`#786f5e`）改为 **`l−58`（`#6f6758`）**——初稿值仅对页面底色合格（4.66:1），代入 `--bg-hover` 跌至 **4.27:1**、`--bg-sunken` 跌至 **4.09:1**，即鼠标悬停行时元数据会跌破 AA。新值三面分别为 5.26 / 4.82 / 4.61:1；
  ② `--accent-focus` 由 `hsla(190 78% 27% / .16)` 改为 **`hsla(193 82% 31% / .16)`**——实测 `#0e7490` 实为 `hsl(193 82% 31%)`，原值是照抄的近似值，并非 accent 本身；
  ③ 新增 `--on-done: #ffffff`（完成勾在 `--c-done` 实心上的前景，5.04:1），以消除组件规则里的 `#fff` 硬编码；
  ④ 明确 `--c-done` / `--accent` 均按**图形**阈值（3:1）核算，并补充"`--border-control` 只对 page / surface 达标，故控件不得放在 hover / sunken 上"；
  ⑤ §2.3 的比较基准由"页面底色"升级为"该元素全部可能表面中最不利者"，并写明悬停底色会主动降低对比度这一陷阱。
- **2026-09-19 v0.3**：**改为唯一事实源，并对齐实测值。** 起因：v0.2 有 12 项数值与 `src/styles.css` 不一致（主色、4 个状态色、圆角、间距、内边距、页面宽、box 尺寸、动效上限全部偏离），文档实际不具约束力。本次修订：
  ① 明确"本文为准、实现服从"及改动流程；
  ② 中性色补齐实测 hex 与对比度列，`--base` 定为 38/12%/97%（暖纸）；
  ③ **删除 `--text-muted`**（对页面底仅 3.60:1，不合格），文本层级收敛为两级——实测比 `l−54` 更浅就无法达到 4.5:1，故不存在第三级灰字；
  ④ **`--border-strong` → `--border-control`**，值由 `l−16`（`#d4d0c9`，1.45:1）改为 `l−43`（`#988d7c`，3.07:1），以满足 WCAG 1.4.11 控件边界 3:1；
  ⑤ **状态色由 4 个收敛为 1 个**：删除 `--c-todo/-doing/-deferred/-scheduled/-doing-tint`，四态统一 `--text-secondary`，只保留 `--c-done`，并把值由 `#6a8f4c`（3.50:1）改为 `#4f7a33`（4.74:1）；
  ⑥ **删除 4 个 chip 色值**，元数据改为 11px 右对齐灰字，并对日期采用 `M/D` 短格式、去掉 `+`/`@` 语法前缀；
  ⑦ 新增 §4.1 行模型（零边框 + 缩进分隔线），说明"卡片墙"的成因与禁止叠加弱信号；
  ⑧ 新增 §2.3 对比度硬约束（以页面底色为基准）与 §7 可机械验证的检查清单；
  ⑨ 阴影由 3 个收敛为 1 个且仅用于浮层；动效上限沿用 150ms 并明确禁止位移与 stagger；
  ⑩ 新增禁止项：表面渐变背景、界面 emoji、界面文案暴露实现细节；
  ⑪ **修正版权年号**：Minimal 的 LICENSE 实为 `Copyright (c) 2020-2024 Steph Ango (@kepano)`，v0.2 误写为 2020-2026 且漏了 `(@kepano)`；同时把"沿用设计系统"表述精确为"沿用四条设计方法，其余数值为本项目自定"。
- **2026-09-19 v0.2**：从"理念参考"升级为"设计系统沿用"。配色改为单一 HSL 基准派生，状态色改为 Minimal 语义色，字体栈加 Inter，checkbox 改圆形小尺寸，完成项无删除线，版权声明改为遵循 MIT。落地于 `src/styles.css`。
- **2026-09-19 v0.1**：初版。收编 `styles.css` 的颜色/字号/间距为 token；确立设计理念、字体栈、动效上限与检查清单。

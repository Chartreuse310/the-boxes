# the-boxes · 开发与易用性测试计划

> 版本 v0.1 · 2026-09-19 · 本文是活文档，每个里程碑结束时回顾更新，变更见文末【变更日志】

## 1. 产品定义

一句话：**本地优先的纯文本有状态 todo 工具**——数据是自己的 Markdown 文件，工具随时可换。

### 核心概念

| 概念 | 定义 |
|---|---|
| **todo** | 最小单元，五种状态：`[ ]` 待办 · `[x]` 完成 · `[/]` 进行中 · `[>]` 顺延 · `[<]` 提前 *（后两者语义待 M0 验证，见 Q1）* |
| **inbox** | 按"提出日期"组织的 todo 流：哪天想到的就落在哪天的文件里 |
| **任务 task** | 一组相关 todo + 一个整体目标；比清单重、比项目轻 |

### 设计原则

1. **数据即文件**：全部数据是人能直接读的 Markdown，无数据库。工具坏了数据不丢，Obsidian / VS Code / `cat` 都能打开。
2. **文件记录事实，视图负责呈现**：文件记录"何时提出、当前状态"；"今天该做什么"由工具计算，不靠手动搬文件。
3. **CLI 先行，UI 后置**：要不要 UI、要什么形态，等积累真实使用数据后再决定。
4. **自己即第一个用户**：每个里程碑的产物必须能管理每天的真实 todo，用不上就不算完成。

### 非目标（v0.x 明确不做）

提醒/推送通知、日历视图、多人协作、原生移动端、AI 功能。重新评估时点：v0.1.0 发布后。

## 2. 总体方法：测试驱动的里程碑循环

每个里程碑都重复同一节奏：

```
明确本阶段范围 → 最小实现 → 真实使用一周 → 记 friction log → 周回顾
        ↑                                                     │
        └──────────  修复"致命"级摩擦后重跑一周  ←──────────────┘
```

三条铁律：

1. **先人肉后代码**：每个新概念先手工用纯文件操作一周，痛了才写代码。
2. **不达标不加功能**：每个里程碑有可量化的毕业标准，未达标只修不加。
3. **痛点只记真实发生的**：friction log 只在真实使用中记录，禁止想象需求。

## 3. 数据格式草案（M0 冻结为 SPEC.md）

```
~/the-boxes/                   # 数据目录（独立于代码仓库）
├── inbox/
│   ├── 2026-09-19.md          # 一天一文件：当天提出的所有 todo
│   └── 2026-09-20.md
├── tasks/
│   └── the-boxes-mvp.md       # 一个任务一个文件：目标 + todos
├── archive/                   # 完结任务的归档（M2 起）
└── .boxes.yml                 # 工具配置（备份目标、ID 规则等）
```

`inbox/2026-09-19.md` 示例：

```markdown
# 2026-09-19

- [ ] 回复 Alice 的邮件
- [/] 写 the-boxes 开发计划
- [x] 晨跑 30 分钟
- [>] 修剪草坪 @2026-09-22
```

`tasks/the-boxes-mvp.md` 示例：

```markdown
# 任务：the-boxes MVP

**目标**：10 月底发布 v0.1.0，且连续 7 天用它管理自己的日常 todo

## todos

- [x] 冻结数据格式（SPEC.md）
- [ ] 实现 add / today / done
- [ ] 第一周 dogfood + friction log
```

## 4. 里程碑

### M0 · 规范冻结 + 人肉版（约 1 周）

**范围**：不写一行产品代码。

- 决策 §7 全部开放问题，产出 `SPEC.md`
- 用任意编辑器手写文件，管理一周的真实 todo
- 建立 `docs/friction-log.md`

**易用性测试**：

- 连续 5 天，所有新 todo 只进 inbox 文件，不许记在别处
- 每天结束自测：不看 SPEC 说出五个状态的含义
- 第 5 天把格式给一个朋友看 60 秒，问他每个状态是什么意思

**毕业标准**：

- [ ] 5 天无中断，五种状态全部自然用到
- [ ] 朋友 60 秒测试中 ≥ 4 个状态被正确理解
- [ ] friction log ≥ 5 条真实记录

### M1 · MVP：inbox 日用流（约 2 周）

**范围**：只做 inbox 的增查改，仅此而已。

```
boxes add "回复 Alice 的邮件"        # 写入今天 inbox
boxes today                         # 今天 + 所有未完结（含顺延项）
boxes done <id>                     # → [x]
boxes doing <id>                    # → [/]
boxes defer <id> [--date 09-22]     # → [>]
boxes pull <id>                     # → [<]
boxes states                        # 状态速查
```

**明确不做**：任务、备份、配置、主题、多 profile。

**易用性测试**：

- dogfood：连续 7 天所有 todo 只进 the-boxes
- 计时：随机记录 10 次"想到 → 敲完回车"耗时，取中位数
- 记忆测试：一周后不看 `--help` 直接用；记不住的命令进 friction log
- 破坏测试：手改文件、加空行、改日期，观察解析是否健壮

**毕业标准**：

- [ ] 添加 todo 中位耗时 ≤ 5 秒，状态切换 ≤ 2 秒
- [ ] 连续 7 天使用无中断，没有一天退回旧工具
- [ ] friction log 无"致命"级未修复项

### M2 · 任务（约 2 周）

**范围**：

```
boxes task new 装修 --goal "11 月底前完成硬装"
boxes task ls                       # 所有任务 + 进度
boxes task show 装修                # 目标 + todos
boxes task add 装修 "约师傅看现场"
```

- 落地 inbox 与任务的关系（Q6 决策的实现）
- 完结任务归档到 `archive/`

**易用性测试**：

- 选 2 个真实任务全程管理：①"the-boxes 开发"本身 ②一个生活任务
- 每周末记录：本周主动打开任务视图几次？不看文件说得出每个进行中任务的目标吗？

**毕业标准**：

- [ ] 2 个真实任务，其一完整走完 立项→拆解→执行→归档
- [ ] 不看文件能说出每个进行中任务的目标
- [ ] inbox 与任务双视图无"同一 todo 看两遍"的混乱感

### M3 · 备份（约 1 周）

**范围**：

- GitHub Private：数据目录即 git 仓库，`boxes sync` = add + commit + push
- WebDAV：`boxes backup --webdav` 打 zip 上传，保留最近 N 份
- 冲突策略 v1：文件级"最后写入优先" + 明确警告，不做三方合并
- `boxes restore` 从备份恢复

**易用性测试**（重点不是功能，是"安全感"）：

- 灾难演练：删掉数据目录 → `boxes restore`，计时并逐项核对
- 双机（工作机 + 个人机）同日使用一天，观察冲突频率与体验
- 实测至少 2 家 WebDAV 服务（如坚果云 + Nextcloud）

**毕业标准**：

- [ ] 恢复演练 ≤ 5 分钟，数据零丢失
- [ ] 连续 7 天自动备份，无一次手动干预
- [ ] 双机冲突场景有明确可理解的处理结果（哪怕粗糙）

### M4 · 打磨与发布 v0.1.0（持续）

**范围**：

- 只修 friction log 中排名前 5 的痛点
- UI 决策：痛点集中在"看/回顾"→ 做只读 Web 仪表盘；集中在"输入"→ 继续 CLI。默认倾向：不做 UI
- 打 tag `v0.1.0`，补 README 与一键安装
- 邀请 3–5 位朋友使用两周

**他人测试脚本**（think-aloud，全程出声思考，限时 10 分钟）：

1. 安装（计时，不提供帮助）
2. 添加一条 todo
3. 完成它，再顺延一条
4. 建立任务并加 2 条 todo
5. 模拟误删文件后从备份恢复

**毕业标准**：

- [ ] 5 项任务无提示完成率 ≥ 80%
- [ ] 安装 ≤ 3 分钟，非程序员可完成
- [ ] 收集 ≥ 3 份完整反馈，形成 v0.2 backlog

## 5. 易用性测试工具箱

### Friction Log（核心资产，贯穿全程）

位置 `docs/friction-log.md`，按月一节，格式：

```
- 2026-09-20 | defer 输完 id 忘了日期格式 | 卡了 30s | 情绪 2/5 | 希望默认明天
```

规则：只记真实发生的事；单条记录 ≤ 30 秒——记录摩擦的工具不能自己成为摩擦。

### 北极星指标

| 指标 | 目标 | 自何时测 |
|---|---|---|
| 添加一条 todo | ≤ 5s | M1 |
| 切换状态 | ≤ 2s | M1 |
| 连续使用天数 | ≥ 7 天/里程碑 | M1 |
| 无提示完成率（他人） | ≥ 80% | M4 |

### 周回顾（每周日 15 分钟，写进日历）

1. 本周 friction 分级：致命 / 烦人 / 可忍
2. 下周只修"致命"，其余进 backlog
3. 对照毕业标准打勾，决定毕业或续期
4. 更新本计划与变更日志

## 6. 风险清单

| 风险 | 缓解 |
|---|---|
| 五状态语义记不住 | `boxes states` 速查；每状态配动词命令别名 |
| 手写与工具混用导致格式漂移 | 解析器容错 + `boxes doctor` 体检命令 |
| 多机并发编辑冲突 | v1 最后写入优先 + 警告；M3 灾难演练覆盖 |
| WebDAV 各家实现差异大 | M3 至少实测 2 家 |
| 计划宏大但没人用 | 铁律 2 不达标不加功能；每阶段强制 dogfood |

## 7. 开放问题（M0 结束前全部决策）

| # | 问题 | 当前倾向 |
|---|---|---|
| Q1 | `[>]` `[<]` 语义 | A：`[>]` 推后 / `[<]` 提前（时间对称，好记）；B：BuJo 式 `[>]` 无定期顺延 / `[<]` 排期到具体日期。M0 手用一周后定 |
| Q2 | 技术栈 | Go（单二进制分发最简）或 TypeScript（未来 Web UI 可复用）。按"愿意长期维护哪个"定 |
| Q3 | 顺延后的行放哪 | 倾向：留在提出日的文件里改状态 + 目标日期（原则 2），视图负责聚合 |
| Q4 | todo 稳定 ID | 短哈希 vs 日期+序号；影响 M3 同步合并，必须 M0 定 |
| Q5 | Obsidian Tasks 兼容 | 读取兼容，写入只用自有子集 |
| Q6 | 任务文件形态 | 一任务一文件 vs 一文件多任务；子任务建议 v1 不做嵌套 |

## 8. 仓库结构演进

```
the-boxes/                # 开源代码仓库
├── PLAN.md               # 本文档
├── SPEC.md               # M0 产出：格式规范
├── docs/friction-log.md  # 摩擦日志
└── src/                  # M1 起：CLI 实现
```

注意：数据目录（§3）独立于代码仓库，M3 备份到 GitHub Private 的是数据仓库，两者不混。

## 变更日志

- **2026-09-19** v0.1：初版计划。含里程碑 M0–M4（各带毕业标准）、易用性测试工具箱（friction log / 北极星指标 / 周回顾 / 他人测试脚本）、风险清单、开放问题 Q1–Q6。实现路径：M0 人肉验证格式 → M1 inbox CLI → M2 任务 → M3 备份 → M4 打磨发布 v0.1.0。
